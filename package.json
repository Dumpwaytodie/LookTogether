{
  "name": "novacast",
  "version": "1.0.0",
  "description": "Screen share + video call + chat — no account needed",
  "main": "server/index.js",
  "scripts": {
    "start": "node server/index.js",
    "dev": "npx nodemon server/index.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "socket.io": "^4.7.2"
  }
}
